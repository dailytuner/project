global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  # Pgwatch2 отправляет метрики сюда
  - job_name: 'pushgateway'
    honor_labels: true
    static_configs:
      - targets: ['localhost:9090']
